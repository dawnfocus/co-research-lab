# Findings

Each finding lives in `F###-short-name/README.md`. A finding is a reviewable claim supported by one or more experiments or sources—not a copy of an experiment log.

Use [`_template/`](_template/) to create a record. Never reuse an ID; revise or supersede an existing finding when evidence changes.
